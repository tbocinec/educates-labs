# Úvod do Kafka Intro workshopu

Vitaj v malom hands-on workshope, kde si v prostredí **Educates** spustíš Apache Kafka broker pomocou **Docker Compose** a vyskúšaš si úplne základnú prácu s topicmi, producerom a consumerom.

## Ciele workshopu

Na konci workshopu budeš vedieť:

- spustiť Apache Kafka broker v Docker kontajneroch,
- vytvoriť Kafka topic cez príkazový riadok,
- posielať textové správy do topicu (producer),
- čítať správy z topicu (consumer).

## Čo budeme používať

V rámci workshopu budeš pracovať s:

- **Terminal** – shell priamo v prehliadači (bash/sh),
- **Editor** – VS Code-like editor v prehliadači,
- **Docker** – docker-in-docker v rámci session,
- `docker compose` – na spúšťanie Zookeeper + Kafka kontajnerov.

## Štruktúra workshopu

Workshop je rozdelený na viac modulov:

1. **Úvod do workshopu** – práve čítaš 🙂
2. **Spustenie Kafka cez Docker Compose** – vytvorenie `docker-compose.yml` a spustenie Kafka stacku.
3. **Práca s Kafka CLI** – vytvorenie topicu, producer a consumer.
4. **Zhrnutie a ďalšie kroky** – nápady, ako workshop ďalej rozširovať.

Keď budeš pripravený, pokračuj na ďalší krok v menu: **„Spustenie Kafka cez Docker Compose“**.
