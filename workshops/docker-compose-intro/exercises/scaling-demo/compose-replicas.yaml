services:
  worker:
    image: alpine:latest
    command: sh -c 'echo "Worker $(hostname) started" && sleep 3600'
    deploy:
      replicas: 3

  web:
    image: nginx:latest
    ports:
      - "8080:80"
