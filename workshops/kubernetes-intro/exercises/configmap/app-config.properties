# =============================================================
# app-config.properties — Sample application configuration file
# =============================================================
# This file will be loaded into a ConfigMap and mounted
# as a file inside a Pod container.
# =============================================================

database.host=db.example.com
database.port=5432
database.name=myapp
log.level=INFO
app.feature.cache=true
