# Stage 1: Build
FROM golang:1.23-alpine AS builder

WORKDIR /app
COPY go.mod .
COPY main.go .

RUN go build -o server main.go

# Stage 2: Runtime
FROM alpine:latest

WORKDIR /app
COPY --from=builder /app/server .

EXPOSE 8080

CMD ["./server"]
